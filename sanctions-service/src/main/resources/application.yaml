server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ragdb?currentSchema=sanctions
    username: raguser
    password: ragpass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: sanctions